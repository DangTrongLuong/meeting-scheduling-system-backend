package com.meeting.schedule_a_meeting.enums;

public enum CallParticipantStatus {
    RINGING,        // Đang đợi người nhận
    ACCEPTED,       // Đã chấp nhận cuộc gọi
    REJECTED,       // Đã từ chối cuộc gọi
    MISSED,         // Bỏ lỡ cuộc gọi
    LEFT            // Đã rời khỏi cuộc gọi
}